'use strict';

var app = app || {};

(function (module) {
  var errorView = {};

  let err = {
    status: 404,
    message: "page not found"
  }

  errorView.initErrorPage = err => {
    $.get`${ENV.
  }


})(app);